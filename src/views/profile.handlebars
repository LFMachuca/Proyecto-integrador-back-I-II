<h1>PROFILE</h1>
<section>
    <div>
    <img src={{user.avatar}} class="object-fit-cover " alt={{user._id}}>
    </div>
    <div>
        <h3>{{user.name}}</h3>
        <p>{{user.date}}</p>
        <p>{{user.city}}</p>
        <p>{{user.email}}</p>
        <a href="/update-user" class="btn btn-warning">Update</a>
    </div>
</section>